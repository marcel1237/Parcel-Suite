# Resultados

Guardar identidade, hashes, `git apply --check`, buildworld/buildkernel,
Kyua/ATF, boot em VM e benchmarks. Não guardar chaves ou imagens de disco.

No estado inicial há apenas validação estática. O build completo e boot ainda
não foram executados.
